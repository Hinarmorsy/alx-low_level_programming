#include <unistd.h>
#include "main.h"

/**
 * _putchar - write the character c to stdout
 * @c: the character ti print
 *
 * Return: on success 1.
 * on error -1 is returned
*/
int _putchar(char);
{
	return (write(1, &c, 1));
}
